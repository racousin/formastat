# Copy to .env (gitignored). Source it or pass to docker compose.

# --- Start here: AI-Studio key (fastest) ---
GEMINI_API_KEY=

# Model the loop drives (formalize + prove)
ULAM_GEMINI_MODEL=gemini-3.1-pro-preview

# --- Grant-billed path: route at Vertex AI on the formastat project ---
# Run the LiteLLM proxy (docker-compose `litellm` service) and point here:
# ULAM_GEMINI_BASE_URL=http://litellm:4000/v1

# Exact UlamAI headless command (confirm in-container; placeholders:
# {latex_file} {name} {out_file}). Only needed for --driver ulam.
# FORMASTAT_ULAM_CMD=ulam prove --input {latex_file} --name {name} --out {out_file}
